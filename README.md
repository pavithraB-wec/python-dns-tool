# Python DNS Lookup Tool

A simple and clean **DNS Resolver tool** built using Python’s socket library.

## 🔍 Features
- Convert **domain → IP address**
- Retrieve all DNS records (IPv4 + IPv6)
- Error handling for invalid domains
- CLI-based tool for learning computer networks

## ▶️ Usage
```bash
python main.py
